namespace Operations
{
    public interface IMathOperations
    {
        int Add(int firstElement, int secondElement);

        double Add(double firstElement, double secondElement, double thirdElement);

        decimal Add(decimal firstElement, decimal secondElement, decimal thirdElement);
    }
}

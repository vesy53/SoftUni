namespace p01._01.Hogswatch
{
    using System;

    class Hogswatch
    {
        static void Main(string[] args)
        {
            int homesToVisit = int.Parse(Console.ReadLine());
            int initialPresents = int.Parse(Console.ReadLine());

            int totalPresents = 0;

            for (int i = 0; i < homesToVisit; i++)
            {
                int presents = int.Parse(Console.ReadLine());

                
            }

            Console.WriteLine(totalPresents);
        }
    }
}
